import { http } from "./rquest";
import { getToken, setToken, removeToken } from "./token";
import { setLocalStorage, getLocalStorage, removeLocalStorage, setSessionStorage, getSessionStorage, removeSessionStorage } from './storage'

export {
    http,
    getToken,
    setToken,
    removeToken,
    setLocalStorage,
    getLocalStorage,
    removeLocalStorage,
    setSessionStorage,
    getSessionStorage,
    removeSessionStorage
}